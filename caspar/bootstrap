#!/bin/sh -e

# $Id: bootstrap,v 1.2 2002-03-01 15:15:06 joostvb Exp $

#
# bootstrap - script to bootstrap the distribution rolling engine
#

set -x

if [ ! -f VERSION ]; then
	./setversion
fi

aclocal \
    && automake --verbose --gnu \
    && autoconf


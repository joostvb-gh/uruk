#!/bin/sh -e

# $Id: bootstrap,v 1.3 2003-02-05 21:57:01 joostvb Exp $

#
# bootstrap - script to bootstrap the distribution rolling engine
#

set -x

if [ ! -f VERSION ]; then
	./setversion
fi

aclocal \
    && automake --add-missing --verbose --gnu \
    && autoconf


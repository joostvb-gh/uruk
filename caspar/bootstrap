#!/bin/sh -e

# $Id: bootstrap,v 1.1 2002-02-26 17:51:15 joostvb Exp $

#
# bootstrap - script to bootstrap the distribution rolling engine
#

set -x

if [ ! -f VERSION ]; then
	chmod a+x setversion
	./setversion
fi

aclocal \
    && automake --verbose --gnu \
    && autoconf

